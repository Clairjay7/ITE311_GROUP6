# Redirect all public folder requests to root directory
<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Redirect /Group6/public/anything to /Group6/anything
    RewriteRule ^(.*)$ ../$1 [R=301,L]
</IfModule>

# Disable directory browsing
Options -Indexes
